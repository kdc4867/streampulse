# 가볍고 빠른 Python 3.9 Slim 버전 사용
FROM python:3.9-slim

# 작업 디렉토리 설정
WORKDIR /app

# 필수 패키지 설치 (gcc는 duckdb 등 컴파일 필요시 대비)
RUN apt-get update && apt-get install -y \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# 의존성 파일 복사 및 설치
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 소스 코드 전체 복사
COPY . .

# 실행 명령어 (runner.py 실행)
CMD ["python", "-m", "src.collectors.runner"]